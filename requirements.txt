# Pip-format requirements (generated from conda-style file).
# System libraries and conda-only packages were removed.
# Install platform-specific `torch` separately (see note below).

accelerate==1.3.0
aiohttp==3.11.10
aiosignal==1.2.0
annotated-types==0.6.0
anyio==4.6.2
attrs==24.3.0
datasets==2.19.1
deprecated==1.2.13
dill==0.3.8
filelock==3.13.1
fsspec==2024.3.1
gmpy2==2.2.1
huggingface_hub==0.24.6
jinja2==3.1.5
markupsafe==2.1.3
multiprocess==0.70.15
networkx==3.4.2
numpy==1.26.4
openai==1.60.1
packaging==24.2
pandas==2.2.3
pydantic==2.10.3
pydantic-core==2.27.1
pyarrow==19.0.0
psutil==5.9.0
python-dateutil==2.9.0
python-dotenv==1.0.1
pytz==2024.1
pyyaml==6.0.2
regex==2024.11.6
requests==2.32.3
safetensors==0.4.5
sympy==1.13.3
tokenizers==0.20.1
tqdm==4.66.5
transformers==4.45.2
typing-extensions==4.12.2
urllib3==2.3.0
yarl==1.18.0
zipp==3.21.0

# Notes:
# - The conda file included many low-level system libraries (BLAS, libtorch, openssl, etc.)
#   which pip does not manage. If your project relies on a specific BLAS/OpenMP/LLVM
#   or GPU-enabled PyTorch build, install those platform-specific dependencies separately.
# - PyTorch: install a compatible `torch` wheel for your platform. Example (CPU):
#     pip install --index-url https://download.pytorch.org/whl/cpu torch==2.5.1
#   For CUDA or other builds, use the command from https://pytorch.org/get-started/locally/.
# - If you want tighter constraints, consider producing a `constraints.txt` from a
#   working environment and using `pip install -r requirements.txt -c constraints.txt`.
